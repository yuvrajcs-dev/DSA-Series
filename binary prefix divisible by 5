class Solution {
    public List<Boolean> prefixesDivBy5(int[] nums) {
        List<Boolean>obj= new ArrayList<>();
        int n=0;
        for(int bit : nums){
            n=(n*2+bit)%5;
            obj.add(n == 0);
        }
        return obj;
        
    }
}
